WasteLess Mobile (Expo)

This folder contains a minimal Expo scaffold for the WasteLess mobile app.

Run locally:

1. Install dependencies: `npm install` inside `apps/mobile`
2. Start Expo: `npm run start`

Integrate with the main project API by pointing the mobile app to the same backend URL and using the JWT auth endpoints at `/api/auth`.
